<%= form_for(follow_list) do |f| %>
  <% if follow_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(follow_list.errors.count, "error") %> prohibited this follow_list from being saved:</h2>

      <ul>
      <% follow_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :follow_id %>
    <%= f.number_field :follow_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
