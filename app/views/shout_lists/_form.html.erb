<%= form_for(shout_list) do |f| %>
  <% if shout_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shout_list.errors.count, "error") %> prohibited this shout_list from being saved:</h2>

      <ul>
      <% shout_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shout %>
    <%= f.text_area :shout %>
  </div>

  <!--><div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div><-->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
